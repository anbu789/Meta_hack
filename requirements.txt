# PharmaVigil — OpenEnv Pharmacovigilance Environment
# FastAPI server
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0.0

# HTTP client (baseline agent + inference.py)
requests>=2.31.0
httpx>=0.27.0

# OpenAI client (baseline agent + inference.py)
openai>=1.0.0
anthropic>=0.25.0
# PyTorch + HuggingFace (inference.py — required for Meta PyTorch hackathon)
#torch>=2.2.0
transformers>=4.40.0

# Statistics (grader_task2, grader_task3)
numpy>=1.26.0
scipy>=1.12.0

# Utilities
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
